#!/usr/bin/env ast

# Lists applications in a domain

if ARGV.length != 2 then
	puts "\n\nUSAGE: #{ARGV[0]} <domainname>"
	return
end

ast.configurationServiceClient.getAllApplicationNames(ARGV[1]).each { |el|

puts "Application: #{el}"

}






